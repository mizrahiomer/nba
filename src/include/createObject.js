export const createTeamObject = (userId, teamId, teamName, teamImg) => {
  return {
    userId,
    teamId,
    teamName,
    teamImg
  };
};
export const createPlayerObject = (userId, playerId, playerName, playerImg) => {
  return {
    userId,
    playerId,
    playerName,
    playerImg
  };
};
